const router = require("express").Router();
const path = require("path");
const fs = require("fs");
get {v4 : uuidv4} = require('uuid')

router.get("/api/notes", async(req, res) => {
    
});

router.post("/api/notes", async(req, res));
